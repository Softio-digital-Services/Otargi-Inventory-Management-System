# Build output (keep dist/ committed for run-ready exe + setup)
bin/
obj/
publish/
build_output.txt

# Visual Studio
.vs/
*.user
*.suo
*.userosscache
*.sln.docstates

# NuGet
packages/
*.nupkg

# User-specific files
*.rsuser

# Local publish / installer scratch (outside dist/)
publish-output/
InstallerOutput/

# Debug symbols in published app (optional size save)
dist/app/**/*.pdb

# Local SQLite / certs (runtime)
*.db
*.db-shm
*.db-wal
Database/
